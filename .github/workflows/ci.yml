name: ci
on: [push]
jobs:
  cibuild:
    name: cibuild
    runs-on: ubuntu-20.04
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v2
      - uses: WillAbides/setup-go-faster@v1
        with:
          go-version: '~1.15.0'
      - run: script/generate --check
      - run: script/test
      - run: script/lint
      - name: sayhi
        run: |
          set -ex
          got="$(script/sayhi)"
          if [ "$got" != "hi2" ]; then
            echo "::error ::wrong output expected 'hi2' but got '$got'"
          fi
